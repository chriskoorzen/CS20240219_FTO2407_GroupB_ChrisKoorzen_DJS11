
export type Episode = {
    showID : string,
    seasonID: number,
    episodeID: number,
};

export type Favorite = {
    date: number,
    showTitle: string,
    episodeTitle: string,
    imgUrl: string
}